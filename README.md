# GPT-2 Text Generator

A local text generation project using HuggingFace Transformers and PyTorch.

## Features
- Uses distilgpt2
- Custom generation parameters
- Runs locally with CPU

## Setup
pip install torch transformers numpy==1.26.4